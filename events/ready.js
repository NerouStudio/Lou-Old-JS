const discord = require('discord.js');
module.exports = {
    name: "ready",
    async execute(interaction, client) {
        console.log(`Бот успешно запущен.`)
        // мэйби пригодится
    }
}